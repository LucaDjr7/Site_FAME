# Vague 2 — Accessibilité / SEO / Perf / UX — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Solder le domaine D6 de l'audit — SEO (metadata, sitemap, robots, hreflang, OpenGraph), accessibilité (dialog/focus, labels, clavier, ARIA, contraste), perf (rAF, reduced-motion, fonts, images, memo) et UX (anti double-submit, loading, feedback).

**Architecture:** Une branche `vague2`, une PR `vague2 → main`. Les modules SEO (`sitemap`/`robots`/`generateMetadata`) sont importables en env `node` → TDD complet. Les changements de composants React (a11y/UX) ne sont pas testables sans jsdom (hors périmètre) → vérification structurelle `tsc`+`lint`+revue+manuel documenté, avec **test de garde** quand une assertion ciblée est possible. Toute chaîne introduite (aria-label) naît i18n.

**Tech Stack:** Next.js 16.2.9 (App Router, `generateMetadata`, `params`=`Promise`), React 19, TypeScript strict, next-intl, Vitest 3 (env `node`).

**Spec de référence :** `docs/superpowers/specs/2026-06-25-vagues-2-4-design.md`. **Audit brut :** `docs/audit-raw/D6-perf-a11y.md`.

## Global Constraints

Verbatim de la spec §7 — chaque tâche les inclut implicitement :

- **i18n** : zéro chaîne UI hardcodée ; toute clé ajoutée existe dans `messages/en.json` **ET** `messages/fr.json`. Toute aria-label/title introduit utilise `t()`.
- **Sécurité** : clés service-role/Dropbox/Resend server-only, jamais `NEXT_PUBLIC_`. Seuls `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_APP_URL` portent ce préfixe.
- **Routing** : lab slug validé (`paris`|`montreal`) dans chaque handler ; invalide → 404.
- **Next.js 16** : `params` = `Promise` → `await params`.
- **Tailwind v4** : ne JAMAIS retirer `@config` de `globals.css`.
- **Aucune** garde `assertLabAccess` ; `Lab='paris'|'montreal'` intact.
- **Gate par tâche** : `npm test` + `npx tsc --noEmit` + `npm run lint` à **0 erreur / 0 warning**.
- **Versioning** : commit atomique `feat:`/`fix:` par tâche, message terminé par `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## ⚠️ Notes d'exécution

- **Harnais node-only** (convention V0/V1) : pas de jsdom/`@testing-library`. Seuls les **modules SEO** (Tasks 1–2) et les **helpers purs** reçoivent un TDD RED→GREEN. Les composants React (Tasks 3–8) sont vérifiés structurellement (`tsc`+`lint`+revue+manuel), avec test de garde où faisable. **Ce qui n'est pas couvert l'est explicitement.**
- **Vérification d'état** : la 1re étape de chaque tâche vérifie le code courant. Un constat déjà résolu → test de garde, pas de ré-implémentation.
- **Base URL** : `process.env.NEXT_PUBLIC_APP_URL` (déjà gardé, B7). Fallback `http://localhost:3000` pour le dev.

---

### Task 1: S2 — `sitemap.ts` + `robots.ts`

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`
- Test (create): `src/app/sitemap.test.ts`
- Test (create): `src/app/robots.test.ts`

**Interfaces:**
- Consumes : `routing.locales` (`['en','fr']`) de `@/i18n/routing` ; `process.env.NEXT_PUBLIC_APP_URL`.
- Produces : `default function sitemap(): MetadataRoute.Sitemap` ; `default function robots(): MetadataRoute.Robots`.

- [ ] **Step 1: Test sitemap (RED)** — `src/app/sitemap.test.ts`

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import sitemap from './sitemap'

beforeEach(() => { process.env.NEXT_PUBLIC_APP_URL = 'https://fame.example' })

describe('sitemap', () => {
  it('couvre les routes publiques pour chaque locale et chaque lab', () => {
    const urls = sitemap().map(e => e.url)
    // home par locale
    expect(urls).toContain('https://fame.example/en')
    expect(urls).toContain('https://fame.example/fr')
    // lab grid + sous-pages publiques par lab/locale
    expect(urls).toContain('https://fame.example/en/paris')
    expect(urls).toContain('https://fame.example/fr/montreal')
    expect(urls).toContain('https://fame.example/en/paris/publications')
    expect(urls).toContain('https://fame.example/en/paris/team')
    expect(urls).toContain('https://fame.example/en/paris/propose')
  })
  it('exclut les routes authentifiées et admin', () => {
    const urls = sitemap().map(e => e.url)
    expect(urls.some(u => u.includes('/admin'))).toBe(false)
    expect(urls.some(u => u.includes('/auth'))).toBe(false)
    expect(urls.some(u => u.includes('/data'))).toBe(false)
    expect(urls.some(u => u.includes('/prompts'))).toBe(false)
  })
})
```

- [ ] **Step 2: Lancer (échec)** — Run: `npx vitest run src/app/sitemap.test.ts` — Expected: FAIL (module introuvable).

- [ ] **Step 3: Implémenter `src/app/sitemap.ts`**

```ts
import type { MetadataRoute } from 'next'
import { routing } from '@/i18n/routing'

const BASE = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'
const LABS = ['paris', 'montreal'] as const
const LAB_PAGES = ['', '/publications', '/team', '/propose'] as const // public only

export default function sitemap(): MetadataRoute.Sitemap {
  const now = new Date()
  const entries: MetadataRoute.Sitemap = []
  for (const locale of routing.locales) {
    entries.push({ url: `${BASE}/${locale}`, lastModified: now, changeFrequency: 'monthly', priority: 1 })
    for (const lab of LABS) {
      for (const page of LAB_PAGES) {
        entries.push({ url: `${BASE}/${locale}/${lab}${page}`, lastModified: now, changeFrequency: 'weekly', priority: page === '' ? 0.8 : 0.6 })
      }
    }
  }
  return entries
}
```

- [ ] **Step 4: Lancer (succès)** — Run: `npx vitest run src/app/sitemap.test.ts` — Expected: PASS.

- [ ] **Step 5: Test robots (RED)** — `src/app/robots.test.ts`

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import robots from './robots'

beforeEach(() => { process.env.NEXT_PUBLIC_APP_URL = 'https://fame.example' })

describe('robots', () => {
  it('autorise / et interdit les zones privées, référence le sitemap', () => {
    const r = robots()
    const rule = Array.isArray(r.rules) ? r.rules[0] : r.rules
    expect(rule?.allow).toBe('/')
    expect(rule?.disallow).toEqual(expect.arrayContaining(['/api/', '/en/admin', '/fr/admin']))
    expect(r.sitemap).toBe('https://fame.example/sitemap.xml')
  })
})
```

- [ ] **Step 6: Implémenter `src/app/robots.ts`**

```ts
import type { MetadataRoute } from 'next'

const BASE = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [{ userAgent: '*', allow: '/', disallow: ['/api/', '/en/admin', '/fr/admin', '/en/auth', '/fr/auth'] }],
    sitemap: `${BASE}/sitemap.xml`,
  }
}
```

- [ ] **Step 7: Lancer + gate** — Run: `npx vitest run src/app/robots.test.ts && npx tsc --noEmit && npm run lint` — Expected: PASS, 0/0.

- [ ] **Step 8: Commit** — `git add -A && git commit` (`feat: sitemap + robots (S2)`).

---

### Task 2: S1/S3/S4/S5 — metadata localisée par page + hreflang + OpenGraph

**Files:**
- Modify: `src/app/[locale]/layout.tsx` (ajouter `generateMetadata` racine : title template, hreflang, OG locale)
- Modify (ajouter `generateMetadata` ou `metadata`) : `src/app/[locale]/page.tsx`, `src/app/[locale]/[lab]/page.tsx`, `.../[lab]/paper/[id]/page.tsx`, `.../[lab]/publications/page.tsx`, `.../[lab]/team/page.tsx`, `.../[lab]/tasks/page.tsx`, `.../[lab]/data/page.tsx`, `.../[lab]/prompts/page.tsx`, `.../[lab]/propose/page.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (namespace `meta`)
- Test (create): `src/app/[locale]/metadata.test.ts`

**Interfaces:**
- Consumes : `getTranslations({ locale, namespace: 'meta' })` (next-intl server) ; `routing.locales`.
- Produces : chaque page exporte `generateMetadata({ params }): Promise<Metadata>`. Le layout racine pose `metadataBase`, `title.template`, `alternates.languages`.

- [ ] **Step 1: Ajouter le namespace `meta`** dans `messages/en.json` ET `messages/fr.json` :

```jsonc
// en.json — "meta": {
"meta": {
  "siteName": "FAME",
  "homeTitle": "FAME — Financial and Monetary Economics",
  "homeDesc": "Independent research labs in Paris and Montréal.",
  "labTitle": "{lab} Lab",
  "labDesc": "Research subjects and progress at the {lab} lab.",
  "publicationsTitle": "Publications — {lab}",
  "publicationsDesc": "Papers and preprints from the {lab} lab.",
  "teamTitle": "Team — {lab}",
  "teamDesc": "Members of the {lab} lab.",
  "tasksTitle": "Tasks — {lab}",
  "proposeTitle": "Propose a subject — {lab}",
  "dataTitle": "Data — {lab}",
  "promptsTitle": "Prompts — {lab}",
  "paperTitle": "{title} — {lab}"
}
```
```jsonc
// fr.json — "meta": {
"meta": {
  "siteName": "FAME",
  "homeTitle": "FAME — Économie financière et monétaire",
  "homeDesc": "Laboratoires de recherche indépendants à Paris et Montréal.",
  "labTitle": "Laboratoire {lab}",
  "labDesc": "Sujets de recherche et avancement du laboratoire {lab}.",
  "publicationsTitle": "Publications — {lab}",
  "publicationsDesc": "Articles et prépublications du laboratoire {lab}.",
  "teamTitle": "Équipe — {lab}",
  "teamDesc": "Membres du laboratoire {lab}.",
  "tasksTitle": "Tâches — {lab}",
  "proposeTitle": "Proposer un sujet — {lab}",
  "dataTitle": "Données — {lab}",
  "promptsTitle": "Prompts — {lab}",
  "paperTitle": "{title} — {lab}"
}
```

- [ ] **Step 2: Test metadata racine (RED)** — `src/app/[locale]/metadata.test.ts`

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { generateMetadata } from './layout'

beforeEach(() => { process.env.NEXT_PUBLIC_APP_URL = 'https://fame.example' })

describe('root generateMetadata', () => {
  it('pose metadataBase, le template de titre et les alternates hreflang', async () => {
    const m = await generateMetadata({ params: Promise.resolve({ locale: 'fr' }) })
    expect(String(m.metadataBase)).toContain('https://fame.example')
    expect((m.title as { template?: string }).template).toContain('%s')
    expect(m.alternates?.languages).toMatchObject({ en: '/en', fr: '/fr' })
    expect(m.openGraph?.locale).toBe('fr')
  })
})
```

- [ ] **Step 3: Lancer (échec)** — Run: `npx vitest run "src/app/[locale]/metadata.test.ts"` — Expected: FAIL (`generateMetadata` non exporté).

- [ ] **Step 4: Ajouter `generateMetadata` au layout racine** — `src/app/[locale]/layout.tsx`, avant le composant :

```ts
import type { Metadata } from 'next'
import { getTranslations } from 'next-intl/server'

const BASE = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params
  const t = await getTranslations({ locale, namespace: 'meta' })
  return {
    metadataBase: new URL(BASE),
    title: { default: t('homeTitle'), template: `%s · ${t('siteName')}` },
    description: t('homeDesc'),
    alternates: { languages: { en: '/en', fr: '/fr' } },
    openGraph: { title: t('homeTitle'), description: t('homeDesc'), type: 'website', locale, siteName: t('siteName') },
  }
}
```

- [ ] **Step 5: Lancer (succès)** — Run: `npx vitest run "src/app/[locale]/metadata.test.ts"` — Expected: PASS.

- [ ] **Step 6: Ajouter `generateMetadata` à chaque page** (lab pages : valider le slug, sinon titre générique). Modèle pour `[lab]/publications/page.tsx` (répliquer pour team/tasks/propose/data/prompts/paper en changeant la clé `meta.*` et, pour paper, en interpolant `{title}` depuis le sujet chargé) :

```ts
import type { Metadata } from 'next'
import { getTranslations } from 'next-intl/server'

export async function generateMetadata({ params }: { params: Promise<{ locale: string; lab: string }> }): Promise<Metadata> {
  const { locale, lab } = await params
  const labLabel = lab === 'paris' ? 'Paris' : lab === 'montreal' ? 'Montréal' : lab
  const t = await getTranslations({ locale, namespace: 'meta' })
  return { title: t('publicationsTitle', { lab: labLabel }), description: t('publicationsDesc', { lab: labLabel }) }
}
```

> Note : `data` et `prompts` (membres only) reçoivent une metadata mais restent hors sitemap (Task 1). Pour `paper/[id]`, charger le sujet (déjà fait dans la page) et passer `{ title: subject.titre, lab: labLabel }`.

- [ ] **Step 7: Gate** — Run: `npm test && npx tsc --noEmit && npm run lint` — Expected: PASS, 0/0. Vérif manuelle documentée : `npm run build` puis `curl -s localhost:3000/sitemap.xml` (smoke) — **à noter dans le rapport**.

- [ ] **Step 8: Commit** — `feat: metadata localisée par page + hreflang + OpenGraph (S1/S3/S4/S5)`.

---

### Task 3: A1/A6/U6 — Modal accessible (dialog, focus-trap, restitution, × i18n)

**Files:**
- Modify: `src/components/ui/Modal.tsx`
- Modify: `messages/en.json`, `messages/fr.json` (clé `common.close`)
- Test (create): `src/components/ui/Modal.guard.test.ts` (garde structurelle légère)

**Interfaces:**
- Consumes : `useTranslations('common')`.
- Produces : le conteneur de dialogue porte `role="dialog"` `aria-modal="true"` + `aria-label`/`aria-labelledby` ; focus déplacé dans la modale à l'ouverture, restitué à l'élément déclencheur à la fermeture ; piège Tab ; `×` porte `aria-label={t('close')}`.

- [ ] **Step 1: Ajouter `common.close`** dans EN (`"close": "Close"`) ET FR (`"close": "Fermer"`).

- [ ] **Step 2: Réécrire `Modal.tsx`** (base actuelle lue — `'use client'`, Escape déjà géré). Ajouter :

```tsx
'use client'
import { useEffect, useRef } from 'react'
import { useTranslations } from 'next-intl'

type Props = { open: boolean; onClose: () => void; children: React.ReactNode; title?: string }

export function Modal({ open, onClose, children, title }: Props) {
  const t = useTranslations('common')
  const panelRef = useRef<HTMLDivElement>(null)
  const triggerRef = useRef<Element | null>(null)

  useEffect(() => {
    if (!open) return
    triggerRef.current = document.activeElement
    const panel = panelRef.current
    panel?.querySelector<HTMLElement>('button, [href], input, textarea, select, [tabindex]:not([tabindex="-1"])')?.focus()
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') { onClose(); return }
      if (e.key !== 'Tab' || !panel) return
      const f = panel.querySelectorAll<HTMLElement>('button, [href], input, textarea, select, [tabindex]:not([tabindex="-1"])')
      if (f.length === 0) return
      const first = f[0], last = f[f.length - 1]
      if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus() }
      else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus() }
    }
    window.addEventListener('keydown', handler)
    return () => {
      window.removeEventListener('keydown', handler)
      ;(triggerRef.current as HTMLElement | null)?.focus?.()
    }
  }, [open, onClose])

  if (!open) return null

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"
      onClick={e => { if (e.target === e.currentTarget) onClose() }}>
      <div ref={panelRef} role="dialog" aria-modal="true" aria-label={title ?? undefined}
        className="rounded-xl shadow-2xl w-full max-w-lg mx-4 max-h-[90vh] overflow-y-auto"
        style={{ background: '#fbf9f3', animation: 'modalIn 0.15s ease' }}>
        {title && (
          <div className="flex items-center justify-between px-6 pt-5 pb-3 border-b" style={{ borderColor: '#eceadf' }}>
            <h2 className="font-serif text-lg" style={{ color: '#1d2b56' }}>{title}</h2>
            <button onClick={onClose} aria-label={t('close')} className="text-xl leading-none" style={{ color: '#7e95d6' }}>×</button>
          </div>
        )}
        <div className="p-6">{children}</div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Test de garde** — `src/components/ui/Modal.guard.test.ts` (assertion textuelle sur le source — le rendu DOM n'est pas testable en node) :

```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
const src = readFileSync(new URL('./Modal.tsx', import.meta.url), 'utf8')
describe('Modal a11y (garde structurelle)', () => {
  it('porte role=dialog + aria-modal', () => { expect(src).toContain('role="dialog"'); expect(src).toContain('aria-modal="true"') })
  it('le × a un aria-label i18n', () => expect(src).toMatch(/aria-label=\{t\('close'\)\}/))
  it('restitue le focus au trigger', () => expect(src).toContain('triggerRef'))
})
```

- [ ] **Step 4: Gate** — `npx vitest run src/components/ui/Modal.guard.test.ts && npx tsc --noEmit && npm run lint`. **Vérif manuelle documentée** : ouvrir une modale au clavier, Tab piégé, Escape ferme, focus revient au bouton déclencheur.

- [ ] **Step 5: Commit** — `feat: Modal accessible — dialog/focus-trap/restitution/× i18n (A1/A6/U6)`.

---

### Task 4: A7 — Toast `role="status"` `aria-live`

**Files:** Modify `src/components/ui/Toast.tsx:27` — sur le conteneur `fixed top-4 …`, ajouter `role="status" aria-live="polite" aria-atomic="true"`.

- [ ] **Step 1:** Éditer le `<div className="fixed top-4 left-1/2 …">` → ajouter les 3 attributs.
- [ ] **Step 2: Garde** — test lisant le source : `expect(src).toContain('aria-live="polite"')`.
- [ ] **Step 3: Gate** + commit `fix: Toast role=status/aria-live (A7)`.

---

### Task 5: A2/A11/U7/U8 — labels de formulaires & contrôles de filtres

**Files (modify):** `lab/AddSubjectModal.tsx`, `tasks/AddTaskModal.tsx`, `team/InviteModal.tsx`, `team/EditMemberModal.tsx`, `publications/AddPublicationModal.tsx` (A2 — chaque `<label>` reçoit `htmlFor="x"`, l'`<input>`/`<textarea>` associé reçoit `id="x"`) ; `paper/CommentsPanel.tsx` (A11 — `<label>`/`aria-label` i18n sur les inputs visiteur) ; sidebars de filtre `lab/FilterSidebar.tsx`, `tasks/TaskFilterSidebar.tsx` (U7 — `aria-label` i18n sur la recherche ; U8 — `aria-pressed` sur les boutons de tri).

**Interfaces:** clés i18n nouvelles dans le namespace du composant (ex. `lab.searchLabel`, `tasks.searchLabel`, `paper.commentNameLabel`, `paper.commentTextLabel`). EN+FR.

- [ ] **Step 1:** Ajouter les clés i18n (EN+FR) pour chaque aria-label/label introduit.
- [ ] **Step 2:** Pour chaque modale : apparier `htmlFor`/`id` (ids uniques par champ). Préserver le style inline existant.
- [ ] **Step 3:** `CommentsPanel` inputs visiteur → `aria-label={t('commentNameLabel')}` / `t('commentTextLabel')`.
- [ ] **Step 4:** Sidebars : `aria-label` i18n sur l'input recherche ; `aria-pressed={active}` sur chaque bouton de tri/bucket.
- [ ] **Step 5: Garde** — test lisant chaque source : présence d'au moins un `htmlFor=` par modale et `aria-pressed` dans chaque sidebar.
- [ ] **Step 6: Gate** + commit `fix: labels htmlFor + aria sur formulaires et filtres (A2/A11/U7/U8)`.

---

### Task 6: A3/A4/A5/A8/A9/A12 + F-HC-03 — éléments interactifs clavier & ARIA

**Files (modify):** `tasks/TaskCard.tsx:23` (A3 — `div onClick` → `<button>` ou `role="button" tabIndex={0} onKeyDown` Enter/Espace) ; `data/DataExplorer.tsx:428` (A4 — `role="tree"` sur le conteneur, `role="treeitem" aria-expanded` sur les nœuds, navigation flèches a minima Enter/Espace) ; `globe/Globe.tsx:321` (A5 — `aria-label={tHome('globeLabel')}` sur le canvas) **et** `:336` (F-HC-03 — le svg overlay : remplacer `aria-label="Lab locations"` par `tHome('globeLabel')`) ; `layout/NavMenu.tsx` (A8 — `aria-controls`/`aria-expanded` sur le bouton menu) ; sous-tâche `×` (A9 — `aria-label={t('delete')}` i18n) ; `layout/LanguageSwitcher.tsx` (A12 — `lang`/`hreflang` sur chaque option).

**Interfaces:** clé `home.globeLabel` (EN: `"Lab locations"`, FR: `"Emplacements des laboratoires"`).

- [ ] **Step 1:** Ajouter `home.globeLabel` (EN+FR) + toute clé delete/menu manquante.
- [ ] **Step 2:** A5 + F-HC-03 : le canvas et le svg overlay utilisent tous deux `tHome('globeLabel')` (un seul libellé i18n, fin du hardcode anglais).
- [ ] **Step 3:** A3 : `TaskCard` cliquable au clavier (préférer `<button>` semantically ; sinon `role="button" tabIndex={0}` + `onKeyDown` déclenchant `onOpen`).
- [ ] **Step 4:** A4 : arbre Dropbox `role="tree"`/`treeitem"`+`aria-expanded`, Enter/Espace pour ouvrir un dossier.
- [ ] **Step 5:** A8 NavMenu `aria-expanded`/`aria-controls` ; A9 `×` sous-tâche `aria-label` ; A12 LanguageSwitcher `lang=`/`hreflang=`.
- [ ] **Step 6: Garde** — test lisant les sources : `Globe.tsx` ne contient plus `aria-label="Lab locations"` (hardcode), `DataExplorer.tsx` contient `role="tree"`, `TaskCard.tsx` contient `onKeyDown` ou `<button`.
- [ ] **Step 7: Gate** + commit `feat: éléments interactifs accessibles au clavier + ARIA + globe label i18n (A3/A4/A5/A8/A9/A12/F-HC-03)`.

---

### Task 7: P1–P7 + A10 — performance & lisibilité

**Files (modify):** `globe/Globe.tsx` (P1 — pause `requestAnimationFrame` sur `document.hidden`/`visibilitychange` ; P5 — couper l'animation si `matchMedia('(prefers-reduced-motion: reduce)').matches`) ; `globals.css` (P5 — wrapper `@media (prefers-reduced-motion: reduce)` neutralisant `fameSpin`/`famePulse`/`fameTwinkle` ; P4 — déplacer les keyframes `drift1-4` depuis `paper/PaperView.tsx:77` sous `/* Paper view animations */`) ; `[locale]/layout.tsx` (P2 — `<link rel="preconnect" href="https://fonts.googleapis.com">` + `gstatic` crossOrigin, dans `<head>`) ; `ui/Avatar.tsx` (P3 — `next/image` quand `photo_url` présent, ou `unoptimized` justifié en commentaire) ; `publications/PublicationList.tsx` (P6 — `useMemo` sur les comptes par type) ; `globe/StarField.tsx` (P7 — commentaire justifiant les 46 SVG ou rien si négligeable — documenter) ; A10 — micro-texte 7px porté à ≥ 11px (ou exempté par contraste documenté).

- [ ] **Step 1:** P4 — couper/coller les `@keyframes drift1..4` de `PaperView.tsx` vers `globals.css` ; supprimer le `<style>` inline ; vérifier que les classes `drift1..4` sont toujours référencées.
- [ ] **Step 2:** P5 — bloc `@media (prefers-reduced-motion: reduce) { .fameSpin, .fameSpinRev { animation: none !important } /* etc. */ }` dans `globals.css` ; garde côté Globe JS.
- [ ] **Step 3:** P1 — dans le `useEffect` rAF du Globe, écouter `visibilitychange` : annuler le rAF quand `document.hidden`, le relancer au retour.
- [ ] **Step 4:** P2 preconnect ; P3 Avatar `next/image` ; P6 `useMemo` ; A10 tailles ; P7 doc.
- [ ] **Step 5: Garde** — test : `globals.css` contient `@keyframes drift1` ET `prefers-reduced-motion` ; `PaperView.tsx` ne contient plus `@keyframes drift`.
- [ ] **Step 6: Gate** + commit `perf: rAF pause + reduced-motion + preconnect + next/image + memo + keyframes globals (P1-P7/A10)`.

---

### Task 8: U1–U5 — robustesse UX des soumissions

**Files (modify):** `propose/ProposeForm.tsx` + `paper/CommentsPanel.tsx` (U1 — `disabled={posting}` sur le bouton submit ; U3 — toast succès après commentaire visiteur) ; routes lourdes (U2 — `loading.tsx` minimal dans `src/app/[locale]/[lab]/` et `.../paper/[id]/`) ; commentaire admin (U4 — `<input>` → `<textarea>`) ; `paper/PaperNav.tsx` (U5 — quand un seul sujet, désactiver les liens `href="#"` ou les masquer).

**Interfaces:** clé i18n `paper.commentPosted` (toast) EN+FR si absente.

- [ ] **Step 1:** U1 — `disabled={posting}` (état déjà présent dans ces composants) sur les boutons submit.
- [ ] **Step 2:** U3 — `addToast(t('commentPosted'), 'success')` après succès ; ajouter la clé EN+FR.
- [ ] **Step 3:** U2 — créer `loading.tsx` (squelette sobre, fond `bg-fame-sand-bg`) dans les 2 dossiers.
- [ ] **Step 4:** U4 textarea ; U5 PaperNav liens neutralisés sur sujet unique.
- [ ] **Step 5: Garde** — test : `loading.tsx` existe dans les 2 dossiers ; `CommentsPanel.tsx` contient `disabled={posting}`.
- [ ] **Step 6: Gate** + commit `feat: robustesse UX soumissions — anti double-submit, loading, feedback (U1-U5)`.

---

## Clôture de vague

- [ ] `npm test && npx tsc --noEmit && npm run lint && npm run build` — tout vert.
- [ ] MAJ `docs/STATUS.md` (bloc Vague 2 : SEO + a11y livrés ; lister la couverture réelle et le manuel non couvert).
- [ ] Revue finale whole-branch (Opus 4.8) via `superpowers:requesting-code-review`.
- [ ] `superpowers:finishing-a-development-branch` → PR `vague2 → main`.
