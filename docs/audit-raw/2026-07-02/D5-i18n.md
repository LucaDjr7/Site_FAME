# D5 — i18n (Sonnet, lecture seule)

Parité en/fr **0 asymétrie** sur 567 clés (test `messages-parity` couvre présence/absence). 0 clé manquante (y compris clés dynamiques `t(\`ns.${x}\`)` toutes retracées). 0 placeholder divergent. 56 paires FR=EN toutes légitimes.

## 🟠 High
- **AddTaskModal affiche l'erreur API brute** (`AddTaskModal.tsx:107-111`) : `err.error ?? t('modal.error')` → texte anglais / message Postgres brut affiché même en session FR (routes `tasks/route.ts` renvoient `error.message`). Fix : mapper codes → clés, ne jamais repasser `err.error`. *(Adjudiqué Medium : défaut i18n/UX, pas sécurité.)*

## 🟡 Medium
- **ICU malformé `{{name}}`/`{{nom}}`** dans `prompts.bodyPlaceholder` (en/fr:488) → en dev affiche la clé brute + erreur `MALFORMED_ARGUMENT` ; en prod affiche littéralement `{{name}}`. Fix : échapper (`'{{name}}'`) ou notation `[name]`.
- **`chunkSubject` : header titre/kicker non localisé** (`src/lib/rag/chunk.ts`) — head calculé une fois sur colonnes plates, réutilisé pour chunks en/fr ; un chunk `en` peut porter un titre FR → contamine le contexte d'Astra. `chunkTask` fait correct. **Nécessite réindexation après fix.**

## ⚪ Low — 18 clés mortes
`lab.modal.*` (11), `paper.openDropbox`, `paper.figurePlaceholder`, `assistant.unanswered`, `graph.filters`, `adminAssistant.none`, `adminAssistant.unansweredTitle`, `auth.passwordTooShort`.

Sain : `localizedSubject`/`localizedTask` (fallback correct partout, y compris `buildGraphData` — le graphe ne fuit PAS de titre brut). Discipline i18n JSX/aria/toast excellente hors AddTaskModal.
